$output.webapp("js","app.js")##
'use strict';

angular.module("angular-lab", ["ngResource", ['ngMaterial']]).run(function(${dollar}log){
            ${dollar}log.debug("starterApp + ngMaterial running...");
          });
